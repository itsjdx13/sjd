# Optional: free Alpha Vantage key (25 requests/day as of August 2026).
# Frontend environment variables are visible to the browser; use only a restricted personal key.
VITE_ALPHA_VANTAGE_API_KEY=

# Optional future sync adapter. The app is fully functional without these values.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
